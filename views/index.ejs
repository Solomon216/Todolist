<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To do List</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div class="box" id="heading">
        <h1> <%=Title%></h1>
    </div>
    
    <div class="box container" >
            <%newitem.forEach(item => {%>
                <form action="/delete" method="post">
                    <div class="item">
                        <input type="checkbox" onchange="this.form.submit()" name="checkbox" id="" value="<%= item.id %>">
                        <p><%= item.name %></p>   
                    </div>
                    <input type="hidden" name="listName" value="<%= Title %>">
                </form>
               
            <%})%>
            <form class="item" action="/" method="post">
                <input type="text" name="task" id="" placeholder="Enter your task" autocomplete="off">
                <button type="submit" name="list" value=<%=Title%> value="<%= Title %>">+</button>
            </form>
    </div>
    
    
    

    
</body>
</html>